# Sprout 项目文档

## 项目概述
基于 Nuxt.js 4.x + TypeScript + UnoCSS + Iconify 的现代 Web 应用。

## 技术栈
- **框架**: Nuxt.js 4.x
- **语言**: TypeScript
- **样式**: UnoCSS 原子化 CSS
- **图标**: Iconify (通过 UnoCSS icons)

## 项目结构

```
sprout/
├── app/
│   ├── pages/           # 页面组件
│   │   └── [页面]/
│   │       ├── index.vue
│   │       └── modules/ # 页面模块
│   ├── components/      # 公共组件
│   ├── layouts/         # 布局组件
│   ├── assets/          # 静态资源
│   └── utils/           # 工具函数
├── configs/             # 配置文件
└── public/              # 公共资源
```

## 开发规范

### 文件命名
- **文件/文件夹**: 小写 + 连字符 (kebab-case)
- 示例：`user-profile.vue`, `nav-search/`

### 图标使用
- 使用 UnoCSS icons：`class="i-mdi-home"`
- 支持所有 Iconify 图标集
- 常用集合：mdi, lucide, tabler, heroicons

### 页面模块化
- 复杂页面拆分到 modules 目录
- 模块文件使用 kebab-case 命名
- 每个模块负责独立功能

## 开发指南

### 新增页面
1. 在 `app/pages/` 创建目录和 `index.vue`
2. 如需模块化，创建 `modules/` 子目录
3. 模块文件命名使用 kebab-case

### 样式开发
1. 优先使用 UnoCSS 原子化类
2. 图标使用 UnoCSS icons
3. 响应式设计优先移动端

### 组件开发
1. 使用 `<script setup lang="ts">` 语法
2. 严格的 TypeScript 类型定义
3. 遵循 kebab-case 文件命名
