# Sprout 项目编码规范

## 技术栈
- Nuxt.js 4.x + TypeScript
- UnoCSS 原子化样式框架
- Iconify 图标库（通过 UnoCSS icons）

## 命名规范

### 文件和文件夹命名
- **统一使用小写 + 连字符 (kebab-case)**
- 示例：`nav-search.vue`, `theme-controller/`, `user-profile.vue`

### Vue 组件命名
- 组件名称使用 PascalCase
- 示例：`<NavSearch>`, `<ThemeController>`, `<UserProfile>`

### TypeScript 类型命名
- 接口、类型：使用 PascalCase
- 示例：`interface NavItem {}`, `type ThemeConfig = {}`

### 变量和函数命名
- 变量、函数：使用 camelCase
- 常量：使用 UPPER_SNAKE_CASE
- 示例：`const navItems = []`, `const MAX_COUNT = 10`

## 图标使用
- 优先使用 UnoCSS icons
- 语法：`i-[集合名]-[图标名]`
- 示例：`i-mdi-home`, `i-lucide-user`, `i-tabler-settings`

## 页面模块化开发
- 页面组件按功能拆分到 modules 子目录
- 结构：`pages/[页面名]/modules/`
- 示例：
  ```
  pages/
    nav/
      index.vue          # 主页面
      modules/
        nav-search.vue   # 搜索模块
        nav-filter.vue   # 筛选模块
  ```

## 代码风格
- 使用 Vue 3 组合式 API
- 使用 `<script setup lang="ts">` 语法
- 优先使用 UnoCSS 原子化类
- 严格使用 TypeScript，避免 `any` 类型
