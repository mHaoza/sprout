# Sprout 项目文档

## 项目概述
Sprout 是一个基于 Nuxt.js 4.x 的现代 Web 应用，使用 Vue 3 组合式 API 和 TypeScript 开发。

## 项目结构

### 核心目录
```
sprout/
├── app/                    # 应用源码
│   ├── assets/            # 静态资源
│   │   ├── data/          # 数据文件
│   │   │   ├── nav/       # 导航数据
│   │   │   └── theme/     # 主题数据
│   │   ├── styles/        # 样式文件
│   │   └── textures/      # 纹理图片
│   ├── components/        # 可复用组件
│   │   ├── filter-chips.vue
│   │   ├── header.vue
│   │   ├── nav-search.vue
│   │   └── theme-controller/  # 主题控制器组件
│   ├── layouts/           # 布局组件
│   ├── pages/             # 页面组件
│   │   ├── about/         # 关于页面
│   │   ├── anime/         # 动漫页面
│   │   ├── demo/          # 演示页面
│   │   ├── home/          # 首页
│   │   └── nav/           # 导航页面
│   └── plugins/           # Nuxt 插件
├── configs/               # 配置文件
├── public/                # 静态资源
└── 配置文件...
```

### 关键组件说明

#### 主题控制器 (theme-controller)
- 路径：`app/components/theme-controller/`
- 功能：管理应用主题切换
- 包含：主题列表配置、主题控制器组件

#### 导航系统
- 数据配置：`app/assets/data/nav/`
- 组件：`app/components/nav-search.vue`
- 页面：`app/pages/nav/index.vue`
- 功能：提供应用导航和搜索功能

#### 样式系统
- 主样式：`app/assets/styles/main.css`
- 重置样式：`app/assets/styles/reset.css`
- 主题样式：`app/assets/styles/theme.css`
- 使用 UnoCSS 作为原子化 CSS 框架

## 技术特性

### 路由配置
- 自定义首页路由映射：`/` → `/home`
- 使用 Nuxt 的文件路由系统
- 支持嵌套路由和动态路由

### 样式方案
- UnoCSS：提供原子化 CSS 类
- CSS 变量：用于主题定制
- 响应式设计：支持移动端适配

### 类型安全
- 完整的 TypeScript 支持
- 严格的类型检查
- 接口定义和类型约束

### 开发工具
- ESLint：代码质量检查（@antfu/eslint-config）
- Nuxt DevTools：开发调试工具
- Vite：快速的开发服务器

## 开发指南

### 本地开发
```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览生产版本
pnpm preview
```

### 新增页面
1. 在 `app/pages/` 目录创建页面文件
2. 使用 `<script setup lang="ts">` 语法
3. 遵循命名规范：文件夹和文件名用小写+连字符

### 新增组件
1. 在 `app/components/` 目录创建组件
2. 组件名使用 PascalCase
3. 导出时使用默认导出
4. 添加适当的 TypeScript 类型定义

### 样式开发
1. 优先使用 UnoCSS 原子化类
2. 自定义样式写在 `<style scoped>` 中
3. 主题相关样式使用 CSS 变量
4. 遵循样式命名规范

### 数据管理
1. 静态数据放在 `app/assets/data/` 目录
2. 使用 TypeScript 定义数据类型
3. 导出配置对象供组件使用

## 部署说明

### 静态生成 (SSG)
```bash
pnpm generate
```

### 服务端渲染 (SSR)
```bash
pnpm build
pnpm preview
```

## 注意事项
- 项目使用 pnpm 作为包管理器
- 开发时确保 TypeScript 类型正确
- 遵循项目的命名规范和代码风格
- 新增功能时考虑组件的可复用性
