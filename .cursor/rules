# Sprout 项目编码规范

## 技术栈
- Nuxt.js 4.x + Vue 3 + TypeScript
- UnoCSS 样式框架
- ESLint (@antfu/eslint-config)
- PNPM 包管理器
- VueUse 组合式API工具库

## 命名规范

### 文件和文件夹命名
- 统一用小写加连字符`-`命名，多个单词用连字符连接
- 示例：`nav-search.vue`, `theme-controller/`, `filter-chips.vue`

### Vue 组件命名
- 组件名称统一用 PascalCase 法命名，多个单词首字母大写
- 示例：`<AppProvider>`, `<RouterView>`, `<FilterChips>`
- iconify 图标组件名称统一用 kebab-case 法命名
- 示例：`<icon-mdi-emoticon />`

### TypeScript 类型命名
- 构造函数、class 类、TS 类型：统一用 PascalCase 法命名
- 示例：`interface NavItem {}`, `type ThemeConfig = {}`

### 变量和函数命名
- 变量、普通函数：统一用 camelCase 法命名
- 示例：`const navItems = []`, `function getThemeList() {}`
- 常量：统一用大写字母命名，多个单词用下划线连接
- 示例：`const MAX_COUNT = 10`, `const DEFAULT_THEME = 'light'`

### 样式命名
- 统一用小写字母命名，多个单词用中划线连接
- 示例：`.nav-container`, `.theme-item`

### API 请求函数命名
- 统一以`fetch`开头，后面跟请求的资源名称
- 示例：`fetchNavList()`, `fetchThemeConfig()`

## 代码规范

### Vue 组件结构
```vue
<template>
  <!-- 模板内容 -->
</template>

<script setup lang="ts">
// 导入
// 类型定义
// 组合式API
// 响应式数据
// 计算属性
// 方法
// 生命周期
</script>

<style scoped>
/* 组件样式 */
</style>
```

### TypeScript 规范
- 严格使用 TypeScript，避免使用 `any` 类型
- 为所有函数参数和返回值定义类型
- 使用接口定义对象结构
- 优先使用 `interface` 而不是 `type`（除非需要联合类型）

### UnoCSS 使用规范
- 优先使用原子化CSS类
- 避免在`<style>`中写样式，尽量使用UnoCSS类
- 复杂样式可以在`theme.css`中定义CSS变量

### 组合式API 规范
- 优先使用 Vue 3 组合式API
- 使用`<script setup>`语法
- 合理使用VueUse工具库
- 响应式数据使用`ref`或`reactive`

### 导入顺序
1. Vue 相关导入
2. 第三方库导入
3. 项目内部导入（组件、工具函数等）
4. 类型导入（放在最后，使用`type`关键字）

### 文件组织
- 页面组件放在`pages/`目录
- 可复用组件放在`components/`目录
- 工具函数放在`utils/`目录（如果有）
- 类型定义放在`types/`目录（如果有）
- 静态资源放在`assets/`目录

## 注释规范
- 复杂逻辑必须添加注释
- 组件和函数使用JSDoc格式注释
- 关键配置项添加行内注释
