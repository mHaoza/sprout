# Sprout 项目 AI 助手配置

## 项目上下文
你正在协助开发基于 Nuxt.js 4.x + TypeScript + UnoCSS + Iconify 的现代 Web 应用。

## 核心技术栈
- **Nuxt.js 4.x**: 全栈 Vue 框架
- **TypeScript**: 类型安全的 JavaScript
- **UnoCSS**: 原子化 CSS 框架
- **Iconify**: 图标库 (通过 UnoCSS icons 使用)

## 编码规范

### 命名规范
- **文件/文件夹(包括Vue组件)**: 严格使用 kebab-case (小写 + 连字符)
- **Vue 组件使用时**: PascalCase
- **变量/函数**: camelCase
- **类型/接口**: PascalCase
- **常量**: UPPER_SNAKE_CASE

### 图标使用指南
- 使用 UnoCSS icons：`class="i-[集合]-[图标名]"`
- 常用图标集：
  - `i-mdi-*` (Material Design Icons)
  - `i-lucide-*` (Lucide)
  - `i-tabler-*` (Tabler)
  - `i-heroicons-*` (Heroicons)

### 页面模块化规范
- 复杂页面功能拆分到 `modules/` 目录
- 目录结构：`pages/[页面名]/modules/[模块名].vue`
- 模块文件使用 kebab-case 命名

## 开发指导原则

### 创建新文件时
1. 严格遵循 kebab-case 文件命名
2. 使用 `<script setup lang="ts">` 语法
3. 添加完整的 TypeScript 类型定义
4. 优先使用 UnoCSS 原子化类

### 使用图标时
1. 优先使用 UnoCSS icons
2. 语法：`<div class="i-mdi-home"></div>`
3. 可调整大小：`class="i-mdi-home text-xl"`

### 页面开发时
1. 简单页面直接在 `index.vue` 实现
2. 复杂页面拆分模块到 `modules/` 目录
3. 模块间通过 props/emit 通信, 需要双向绑定使用defineModel

### 样式开发时
1. 优先使用 UnoCSS 原子化类
2. 避免自定义 CSS, 除非必要
3. 响应式设计：`sm:`, `md:`, `lg:` 前缀

## 代码模板

### Vue 页面组件
```vue
<template>
  <div class="container mx-auto p-4">
    <!-- 页面内容 -->
  </div>
</template>

<script setup lang="ts">
// 类型定义
interface PageData {
  // 定义数据结构
}

// 响应式数据
const data = ref<PageData>({})
</script>
```

### 页面模块组件
```vue
<template>
  <section class="module-container">
    <!-- 模块内容 -->
  </section>
</template>

<script setup lang="ts">
interface Props {
  // 属性定义
}

const props = defineProps<Props>()
</script>
```

记住：始终遵循项目的命名规范和技术栈约束，优先使用 UnoCSS icons 和原子化样式。
